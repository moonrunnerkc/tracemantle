# Pre-rename old-name audit

Historical snapshot generated after implementation and before the repository rename; source tree and historical fixtures were reviewed. Quoted current-link statements below describe that earlier state. Active repository links have since been updated; see [rename checklist](../rename-checklist.md). Verification outputs are intentionally excluded because they contain baseline package names and local workspace paths. The untouched supplied plan retains its required filename. The ignored scoped `.github/CLAUDE.md` is the pre-existing instruction source, not a product artifact.

### .gitignore

Classification: baseline/migration tooling or current repository reference.

- Line 26: `.skillcheck-history.json`

### CHANGELOG.md

Classification: historical changelog, case study or evidence.

- Line 14: `- TraceMantle is the canonical distribution, package and CLI (1.6.0). The old distribution must be uninstalled before installing the renamed package; no 'skillcheck' executable is installed. Historical v1 schema identifiers remain unchanged.`
- Line 20: `- Tests for every rejection path in 'skillcheck.toml' loading. Unknown keys, wrong value types, and malformed TOML all raised 'ConfigError', but nothing asserted that the message names the offending key, so a misconfiguration meant reading the loader source to find out which key was wrong. Config discovery is covered too: nearest config wins, a file anchor resolves, and the walk stops at a repository root.`
- Line 32: `- Every user-controlled file goes through one guard, 'io_limits.read_guarded_text', before a parser sees it: size measured with 'stat' so an oversized file is never read, then UTF-8 validity, then a control-character policy. Ingest responses, the history ledger, and 'skillcheck.toml' are all routed through it. Control characters other than tab, newline, and carriage return are refused, since a NUL or a raw ESC in structured input is either a binary file handed to a text parser or an attempt to smuggle terminal escapes into a diagnostic. C1 code points are deliberately allowed through, being ordinary text that 'sanitize_ingested_text' already neutralizes at display.`
- Line 36: `- CLI mode flags, their groups, and every incompatibility are declared once in the new 'skillcheck.modes' and the conflict checks are generated from it. The rules were previously spread across four places in 'cli.py': a pairwise table, two hand-written dicts mapping flags to argparse destinations, a multi-emit branch, and a ledger check in 'main' outside the conflict function entirely, so adding a mode meant editing all four. Emit-versus-emit, emit-versus-ledger, and ledger-versus-ledger conflicts now follow from the group each flag declares; emit-versus-augment pairs stay listed individually because not every pair is disallowed. 'find_mode_conflict' returns the message instead of printing and exiting, so the table is testable in process. No behavior change: all 45 pairs of the ten mode flags were captured from the CLI first ('tests/fixtures/mode_conflicts.json', regenerated by 'scripts/regen_mode_conflict_fixture.py') and the table reproduces every exit code and every stderr byte for byte, including the 15 pairs that legitimately combine.`
- Line 37: `- Every file skillcheck reads from disk is bounded by one shared guard in the new 'skillcheck.io_limits'. Only ingested agent responses were capped before; the SKILL.md, the '.skillcheck-history.json' ledger, and 'skillcheck.toml' were read with a bare 'read_text()'. In CI running over a fork's pull request all three arrive from the branch under test, so an oversized one was read fully into memory before any rule ran. Caps are 4 MiB for a SKILL.md, 8 MiB for a ledger, 1 MiB for a config, and the existing 5 MiB for an ingest payload, each measured with 'stat' before the read. 'enforce_size_cap' takes the caller's exception type, so 'ParseError', 'LedgerError', and 'ConfigError' keep their own remediation text.`
- Line 39: `- Coverage now follows the CLI subprocesses. Most CLI tests shell out to 'python -m skillcheck' on purpose, since exit codes and stdout are the contract and only a real process exercises them, but the tracer does not follow a subprocess by default. 'cli.py' and 'commands.py' therefore measured 34% and 14% while being the most exercised code in the suite. 'parallel = true' plus 'COVERAGE_PROCESS_START' set from 'tests/conftest.py' fixes it: 'cli.py' 34% to 77%, 'commands.py' 14% to 61%, '__main__.py' 0% to 100%, total 82% to 90%.`
- Line 44: `- A ledger or a 'skillcheck.toml' containing non-UTF-8 bytes crashed with an uncaught 'UnicodeDecodeError' traceback and exit 1. The config case was the worse of the two: 'find_config' discovers 'skillcheck.toml' by walking up from the scanned path, so a poisoned file sitting next to a skill broke any scan of that tree with no flag involved. Both now exit 2 with a diagnostic naming the file, the offending byte, and its offset.`
- Line 55: `- 'release.yml' repoints the 'v1' moving major tag after a successful publish, in a separate job that holds the only 'contents: write' scope in the workflow. Moving it was a manual step in the release checklist and was missed for v1.3.0 and v1.4.1, so 'uses: moonrunnerkc/skillcheck@v1' resolved to v1.2.3 source from May 7 until now. Prerelease tags that the 'v*.*.*' trigger glob also matches are skipped, and the first release of a new major line creates the tag instead of updating it.`
- Line 63: `- The duplicate 'SKILL.md' at the repository root. The self-host skill lives at 'skills/skillcheck/SKILL.md', which is what the coherence tests, the self-host suite, the fixture regenerator, and 'make verify-release' all read. The root copy was covered by none of them and had drifted to version 1.3.0 with the older description. 'tests/test_version_coherence.py' now asserts exactly one tracked 'SKILL.md' outside 'tests/fixtures'.`
- Line 74: `- '[tool.ruff]' and '[tool.mypy]' configuration in 'pyproject.toml'. Ruff lints 'src' and 'tests' with an explicit 'E, F, I, UP, B' selection at line length 127 ('E501' ignored for unavoidable long string literals in JSON fixtures and schema text). Mypy runs 'strict' against 'src/skillcheck' under 'python_version = "3.10"', with an 'ignore_missing_imports' override for the untyped 'tiktoken' dependency and the 'tomllib' 3.11+ stdlib backport branch. The source was brought clean under both with real annotations, not blanket ignores.`
- Line 75: `- Test coverage measurement via 'pytest-cov' (added to the 'dev' extra). '[tool.coverage.run]' and '[tool.coverage.report]' configure the run, and 'addopts' in '[tool.pytest.ini_options]' adds '--cov=skillcheck --cov-report=term-missing --cov-fail-under=68', so the floor is enforced on every local run and in CI (which runs the same 'pytest'). The floor sits a few points under the ~72% measured on CPython 3.10 to absorb matrix variance: the CLI modules run in subprocesses the in-process tracer does not see, the 'tomllib' vs fallback-parser branch flips between Python 3.10 and 3.11+, and a few tests skip on Windows.`
- Line 80: `- CI 'lint' job now enforces 'ruff check src tests' and 'mypy src/skillcheck' (strict) on every push and pull request, replacing the prior 'compileall'-only check. Either tool reporting a finding fails the job.`
- Line 81: `- 'cli.py' split to separate argument wiring from command execution. Parser construction, 'skillcheck.toml' application, mode-conflict dispatch, and 'main' stay in 'cli.py'; the per-mode handlers (emit prompts and graphs, '--show-history', the default validation pipeline) and the path and ingest IO helpers move to a new 'skillcheck.commands' module. 'skillcheck.cli:main' and the 'skillcheck' console script are unchanged. Pure refactor, no behavior change.`
- Line 89: `- Dead code removed: the unused 'core/reporter.py' module (the CLI renders through 'formatters.py') and the 'merge_critique_diagnostics' shim (identical to 'merge_diagnostics', now called directly). No public CLI or 'skillcheck.validate' behavior changes.`
- Line 107: `- 'skillcheck.toml' handling improved: config type errors now name the offending value; the Python 3.10 fallback parser no longer truncates a value at a '#' inside quotes; 'find_config' stops ascending at a '.git' repository root or the user's home instead of walking to the filesystem root; and the CLI prints which config file it loaded (to stderr) when one is found.`
- Line 108: `- History ledger writes are more durable: 'save_ledger' now flushes and 'fsync's the temp file before the atomic 'os.replace', and 'load_ledger' sweeps stale '.skillcheck-tmp-*' files left behind by an interrupted write. The module documents its single-writer assumption (no file locking).`
- Line 125: `- '--history' now fans out across every target path instead of silently writing only when exactly one path is supplied. Each SKILL.md still gets its own per-skill '.skillcheck-history.json' next to it. '--fail-on-regression' escalates to exit 1 when any target regresses.`
- Line 128: `- 'action.yml' install step now prefers the action's own checkout when 'inputs.version' is empty and 'pyproject.toml' is present at '$GITHUB_ACTION_PATH'. This pins both 'uses: moonrunnerkc/skillcheck@<tag>' and 'uses: ./' dogfood jobs to the tag's source, closing the previous PyPI/tag drift window. When 'inputs.version' is set, the action still installs from PyPI at that pin. The PyPI range install remains as a fallback for environments where the checkout is absent.`
- Line 133: `- Build provenance attestations on tagged releases. The CI 'package' job now invokes 'actions/attest-build-provenance@v1' against the built wheel and sdist when the ref starts with 'refs/tags/v'. Consumers can verify with 'gh attestation verify dist/skillcheck-*.whl --owner moonrunnerkc' before installing. Documented in the README's new '## Releases' section.`
- Line 136: `- Published JSON Schema (Draft 2020-12) files for the agent IO contracts: 'src/skillcheck/schemas/critique-v1.json' and 'src/skillcheck/schemas/graph-v1.json'. Both ship with the wheel. 'skillcheck.agents.SCHEMAS' maps '"critique-v1"' and '"graph-v1"' to the on-disk paths so callers can validate agent responses before invoking '--ingest-critique' or '--ingest-graph'. The schemas mirror the parser-enforced required fields, severity enum, kind enums, and score ranges; 'test_published_schemas.py' guards drift between the schema files and the parsers.`
- Line 137: `- '[frontmatter] reserved_words' config key in 'skillcheck.toml'. Replaces the default '('anthropic', 'claude')' reserved-word list that powers 'frontmatter.name.reserved-word'. An empty array reverts to the defaults so orgs cannot silently disable the check. Example: 'reserved_words = ["acme", "internal"]'.`
- Line 138: `- 'references.broken-link' and 'references.depth-exceeded' now also scan HTML anchor tags ('<a href="...">') and inline backtick spans that contain a directory separator (e.g. '' 'scripts/foo.py' ''). The backtick extractor is shared with the capability graph ('skillcheck.core.extract_backtick_refs') so the two extractors agree on what counts as a path-like token. Fenced code blocks ('''' ''' '''' blocks) are stripped before backtick scanning so command samples do not produce false positives, and bare filenames ('report.json' with no directory) are excluded because they are typically output mentions, not references.`
- Line 157: `- TOML config: 'strict-all = true' is now accepted in 'skillcheck.toml'.`
- Line 164: `- 'tiktoken' action input in 'action.yml'. Set 'tiktoken: true' to install 'skillcheck[tiktoken]'.`
- Line 171: `- 'action.yml' install line tightened from unpinned 'skillcheck' to 'skillcheck>=1.2,<2'. Python version pinned to '3.12' for the setup-python step.`
- Line 180: `- 'action/entrypoint.py' (unused since v1.2.3; the composite action runs skillcheck directly).`
- Line 188: `- '.pre-commit-hooks.yaml': adds a 'skillcheck' hook for pre-commit, matching 'SKILL.md' files and passing filenames to the CLI.`
- Line 195: `- 'action.yml' simplified to a two-step composite action that installs skillcheck via pip and runs it directly. The Python entrypoint ('action/entrypoint.py') is no longer invoked; '--format github' handles PR annotations natively. The 'format' input defaults to 'github' (was 'json', which was ignored at runtime).`
- Line 202: `- The Python entrypoint ('action/entrypoint.py') for annotation parsing and step summary generation is no longer used by the action. The action runs skillcheck directly.`
- Line 212: `- TOML config: 'strict-cursor = true' is now accepted in 'skillcheck.toml'.`
- Line 230: `- 'template.detected' info-level rule and 'src/skillcheck/template_detection.py' module.`
- Line 232: `- Config support for '[frontmatter] extension_fields' in 'skillcheck.toml'.`
- Line 249: `- Frontmatter rule implementation split into smaller modules while preserving 'skillcheck.rules.frontmatter' imports.`
- Line 250: `- Root 'SKILL.md' restored so 'skillcheck SKILL.md' self-validation works from the repository root.`
- Line 264: `- 'scripts/summarize_batch.py' and 'tests/test_batch15_summarize.py': maintainer-facing tool that consumes a directory of skillcheck batch-run artifacts (one directory per repo, one subdirectory per skill, paired '*.json' / '*.txt' reports per phase) and writes 'summary.csv' plus 'findings.md'. Invoked as 'python scripts/summarize_batch.py <batch_dir>'. Not exposed as a console script, not wired into the GitHub Action; the action runs skillcheck against one path, this consumes outputs across many. Documented under Maintainer Notes in the README.`
- Line 269: `- 'action.yml' install step pins 'skillcheck>=1.0.1' so consumers fail loudly on unpublished v1 features instead of silently running v0.2.0.`
- Line 273: `- README field-test citations: replaced seven gitignored 'runs/...' path references with the exact 'skillcheck' commands needed to reproduce each finding. Readers can now verify the claims without access to private artifacts.`
- Line 278: `- Top-level 'git-commit-crafter' SKILL.md from the repo root. It was unrelated to skillcheck and confused first-time readers; the canonical example lives at 'skills/skillcheck/SKILL.md'.`
- Line 290: `- 'action.yml' 'format' input description clarified: accepted but ignored at runtime; the action always invokes skillcheck with '--format json'.`
- Line 297: `- 'skillcheck.toml' config loading: top-level defaults for format, thresholds, target agent, strict VS Code mode, skip flags, ignored rule prefixes, graph analysis, semantic mode, history, and agent variants.`
- Line 301: `- 'tests/test_v1_completion.py': covers '--format md', '--format agent', '--agent-reason', '--semantic' graph enabling, '--activation-hypotheses' JSON, 'skillcheck.toml' loading, and source/confidence in JSON output.`
- Line 311: `- 'skills/skillcheck/SKILL.md': skillcheck's own SKILL.md, validating the tool against itself. Passes symbolic, graph, critique, and history validation with zero errors and zero warnings. Serves as the worked example for the Rules table in the README.`
- Line 314: `- 'Makefile' with 'regen-self-host-fixtures' target: runs the regen script against 'skills/skillcheck/SKILL.md'.`
- Line 315: `- '--history' flag: appends a validation record to the per-skill '.skillcheck-history.json' ledger next to the SKILL.md file. Off by default; existing invocations see no behavior change. Incompatible with emit modes.`
- Line 338: `- **GitHub Action**: composite action ('moonrunnerkc/skillcheck@v0.2.0') with PR annotations, job summary table, and JSON output. All CLI flags exposed as action inputs. Three lines of YAML to add to any CI pipeline.`
- Line 339: `- **'__main__.py' entry point**: 'python -m skillcheck' now works as an alternative to the console script.`

### CONTRIBUTING.md

Classification: current repository URLs and migration documentation.

- Line 36: `Publishing is disabled unless the owner enables 'TRACEMANTLE_PUBLISH_ENABLED' and configures the new distribution's trusted publisher. See [rename-checklist.md](docs/rename-checklist.md). Do not upload a TraceMantle artifact to the legacy SkillCheck project. Local checks do not establish remote CI success or a public release.`

### Makefile

Classification: baseline/migration tooling or current repository reference.

- Line 44: `@if grep -n "moonrunnerkc/skillcheck@v0" README.md; then echo "FAIL: @v0 reference in README"; exit 1; fi`

### README.md

Classification: current repository URLs and migration documentation.

- Line 19: `Requires Python 3.10 or later. The required runtime dependencies are PyYAML and, on Python 3.10 only, tomli. See [migration instructions](docs/migration.md) before replacing the old SkillCheck distribution. The default install creates no 'skillcheck' executable.`
- Line 51: `Use '[tool.tracemantle]' in 'pyproject.toml', or 'tracemantle.toml'. Legacy 'skillcheck.toml' and '[tool.skillcheck]' are accepted with a stderr deprecation notice. Precedence is defaults, legacy fields, canonical fields, explicitly supplied CLI fields. Explicit default values such as '--format text' win. Settings are immutable and scoped to one project root; ambiguous mixed-root scans are rejected unless '--config' supplies a single explicit configuration.`
- Line 82: `tracemantle migrate-history path/to/skill/.skillcheck-history.json --store evidence/legacy`
- Line 95: `The repository remains [moonrunnerkc/skillcheck](https://github.com/moonrunnerkc/skillcheck) until the owner renames it. The composite [Action](action.yml) installs from its own checkout. Use 'uses: ./' to test the unpublished source. [Trusted comparison workflow](.github/workflows/compare.yml) installs only the trusted tool and reads candidate files without execution.`
- Line 101: `- repo: https://github.com/moonrunnerkc/skillcheck`

### REMEDIATION-EVIDENCE.md

Classification: historical changelog, case study or evidence.

- Line 3: `Baseline: commit '6a477c2', skillcheck v1.4.0.`
- Line 4: `Toolchain baseline (before any change): '786 passed', 'ruff check src tests' clean, 'mypy src/skillcheck' clean.`
- Line 19: `$ printf -- '---\njust a string\n---\nbody\n' > /tmp/p/SKILL.md && skillcheck /tmp/p/SKILL.md`
- Line 30: `- 'src/skillcheck/parser.py': after 'yaml.safe_load', coerce 'None' to '{}', then raise 'ParseError' naming the actual type and path when the value is not a dict.`
- Line 36: `$ skillcheck /tmp/p/SKILL.md`
- Line 43: `'skillcheck.parser.parse' now raises 'ParseError' (caught in 'core/symbolic.py:44' → clean 'parse.error' diagnostic). No traceback.`
- Line 55: `$ skillcheck /tmp/t/iso-dates/SKILL.md --format json   # description: "Formats [ISO] dates ..."`
- Line 60: `- 'src/skillcheck/template_detection.py': escape '...' to '\.\.\.' in the bracketed-placeholder pattern.`
- Line 83: `$ skillcheck /tmp/multi --ingest-critique tests/fixtures/critique/response_warnings.json --format json`
- Line 90: `- 'src/skillcheck/commands.py': 'run_validation' rejects '--ingest-critique'/'--ingest-graph' combined with more than one resolved path, printing an error naming the active flag(s) and the path count, exit 2.`
- Line 97: `$ skillcheck /tmp/multi --ingest-critique .../response_warnings.json`
- Line 100: `$ skillcheck /tmp/multi --ingest-graph .../response_clean.json`
- Line 103: `$ skillcheck /tmp/multi/skill-one/SKILL.md --ingest-critique .../response_warnings.json   # single path still works, EXIT=0`
- Line 121: `- 'src/skillcheck/core/history.py': after 'json.loads', assert root is a dict; validate 'version == LEDGER_SCHEMA_VERSION' (naming both versions on mismatch); assert 'runs' is a list; wrap the per-entry loop in 'except TypeError' so a malformed entry raises 'LedgerError' with the "delete it and re-run" remediation.`
- Line 129: `ledger_bad_version    -> LedgerError: ... has schema version 999, but this skillcheck expects version 1. ...`
- Line 131: `$ skillcheck /tmp/hist/SKILL.md --show-history   # EXIT=1, clean stderr, no traceback`
- Line 143: `$ skillcheck /tmp/dup/SKILL.md --analyze-graph   # allowed-tools: [Bash, Bash]`
- Line 152: `- 'src/skillcheck/core/graph.py': dedupe tool names with 'dict.fromkeys()' (order-preserving) before node creation, filtering non-str items first so nested YAML cannot break the dedupe.`
- Line 180: `$ skillcheck /tmp/enc/SKILL.md --emit-graph`
- Line 182: `skillcheck.parser.ParseError: File is not valid UTF-8: /tmp/enc/SKILL.md   (traceback, exit 1)`
- Line 183: `$ skillcheck valid_basic.md --ingest-critique /tmp/enc/response.json`
- Line 189: `- 'src/skillcheck/commands.py': new '_parse_or_exit(path)' helper (prints the ParseError to stderr, exit 1); routed all emit and re-parse sites through it ('emit_graph', 'emit_critique_prompts', 'emit_graph_prompts', 'emit_agent_reason_packet', 'emit_activation', the '--ingest-*' and '--analyze-graph' re-parses, and the '--history' re-parse). 'read_ingest_raw' now also catches 'UnicodeDecodeError' (exit 2). The score-breakdown re-parse was already inside 'try/except Exception'.`
- Line 222: `- 'src/skillcheck/rules/compat.py': add '_CODEX_DATA_DATE', use it at the Codex provenance label, mention it in the module docstring.`
- Line 237: `$ mypy src/skillcheck`
- Line 265: `- 'src/skillcheck/agents/_ingest.py' (new): 'sanitize_ingested_text' escapes C0/DEL/C1 control chars to a visible inert form.`
- Line 266: `- 'src/skillcheck/core/semantic.py': sanitize 'missing_context', contradiction fields, and finding fields at diagnostic construction.`
- Line 267: `- 'src/skillcheck/core/graph_render.py': sanitize node names/descriptions in the text render only (JSON render left raw; 'json.dumps' escapes control chars).`
- Line 288: `- 'src/skillcheck/agents/_ingest.py': 'MAX_INGEST_BYTES' (5 MiB), 'MAX_INGEST_LIST_ITEMS' (10000), 'enforce_list_cap'.`
- Line 289: `- 'src/skillcheck/commands.py': 'read_ingest_raw' rejects payloads over the byte cap (file via 'stat', stdin via bounded read), naming the actual size and the cap, exit 2.`
- Line 290: `- 'src/skillcheck/agents/parser.py': cap findings/missing_context/contradictions.`
- Line 291: `- 'src/skillcheck/agents/graph_parser.py': cap capabilities/inputs/outputs/edges.`
- Line 303: `$ grep -c additionalProperties src/skillcheck/schemas/*.json   ->  critique: 0, graph: 0`
- Line 308: `- 'src/skillcheck/schemas/critique-v1.json': 'additionalProperties: false' on the top-level object, findings items, contradictions items (3 total).`
- Line 309: `- 'src/skillcheck/schemas/graph-v1.json': same on the top-level object and all four node-collection items (5 total).`
- Line 314: `$ grep -c '"additionalProperties": false' src/skillcheck/schemas/*.json  ->  critique: 3, graph: 5`
- Line 361: `Owner 'moonrunnerkc', Repository 'skillcheck', Workflow filename 'release.yml', Environment 'pypi'.`
- Line 435: `Successfully built skillcheck-1.4.0.tar.gz and skillcheck-1.4.0-py3-none-any.whl`
- Line 449: `$ mypy src/skillcheck`
- Line 480: `$ ruff check src tests  ->  All checks passed!    $ mypy src/skillcheck  ->  Success`
- Line 490: `- 'src/skillcheck/agents/_ingest.py': added 'require_field(obj, key, expected_type, *, error_cls, context)'`
- Line 492: `- 'src/skillcheck/agents/parser.py', 'graph_parser.py': each keeps a one-line '_require_field' that binds`
- Line 507: `$ ruff check src tests  ->  All checks passed!    $ mypy src/skillcheck  ->  Success`
- Line 516: `against the choice sets). So a 'skillcheck.toml' with an invalid value reaches and is caught by these checks:`
- Line 518: `$ printf 'format = "bogus"\n' > skillcheck.toml`
- Line 519: `$ skillcheck SKILL.md --config skillcheck.toml`
- Line 520: `skillcheck: error: format must be one of: text, json, md, agent, github`
- Line 527: `Files touched: deleted 'src/skillcheck/core/reporter.py'; removed 'reporter' from 'core/__init__.py' imports and`
- Line 544: `$ ruff check src tests  ->  All checks passed!    $ mypy src/skillcheck  ->  Success (46 source files)`
- Line 575: `'mypy src/skillcheck -> Success'.`
- Line 579: `Finding: CI ran 'ruff check src tests' and mypy scoped to 'src/skillcheck', leaving`
- Line 591: `- 'pyproject.toml': '[tool.mypy] files' now lists 'src/skillcheck' plus the two checked-in scripts.`
- Line 595: `Note: 'scripts/skillcheck_case_study_report.py' was a pre-existing untracked session artifact. Because the`
- Line 609: `$ mypy src/skillcheck          ->  Success: no issues found in 48 source files`
- Line 623: `::error ...title=skillcheck: frontmatter.name.max-length::Name exceeds 64 characters (got 82)%3A 'this-is-a-very-long-...'`
- Line 627: `- 'src/skillcheck/formatters.py': split '_escape_data' (message: '%', CR, LF only) from '_escape_property'`
- Line 631: `assertions now expect 'skillcheck%3A'; the special-char message expectation is '100%25%0D%0A:,'.`
- Line 635: `::error file=... ,title=skillcheck%3A frontmatter.name.max-length::Name exceeds 64 characters (got 82): 'this-is-a-very-long-...'`
- Line 647: `- 'src/skillcheck/rules/frontmatter_fields.py': replaced the regex scan with a YAML event walk`
- Line 663: `- 'src/skillcheck/core/history_io.py': 'save_ledger' now 'flush()' + 'os.fsync(f.fileno())' before 'os.replace';`
- Line 664: `'load_ledger' sweeps '.skillcheck-tmp-*' via '_sweep_stale_tmp_files'; the module docstring documents the`
- Line 681: `- 'src/skillcheck/config_loader.py':`
- Line 687: `- 'src/skillcheck/cli.py': '_apply_config' prints 'Loaded config from {path}' to stderr when a config is found.`
- Line 695: `4. $ skillcheck /tmp/cfg2/SKILL.md ...  (stderr) Loaded config from /tmp/cfg2/skillcheck.toml`
- Line 736: `Bumped to '1.4.1' in 'pyproject.toml', 'src/skillcheck/__init__.py', 'skills/skillcheck/SKILL.md' (self-host`
- Line 749: `$ mypy src/skillcheck`
- Line 757: `Successfully built skillcheck-1.4.1.tar.gz and skillcheck-1.4.1-py3-none-any.whl   # build check now actually runs`

### docs/case-study-silent-skill-failure.md

Classification: historical changelog, case study or evidence.

- Line 63: `## How skillcheck Catches It`
- Line 65: `Running 'skillcheck' against the mismatched file:`
- Line 68: `$ skillcheck deploy/SKILL.md`
- Line 107: `$ skillcheck deploy-staging/SKILL.md`
- Line 127: `The fix isn't just running 'skillcheck' once. It's adding it to CI so this class of bug can never ship:`
- Line 133: `pip install skillcheck`
- Line 134: `skillcheck .github/skills/ --quiet`

### docs/case-study-v1-real-world-runs.md

Classification: historical changelog, case study or evidence.

- Line 20: `skillcheck /tmp/anthropics-skills/skills/ --format text`
- Line 23: `skillcheck /tmp/anthropics-skills/skills/mcp-builder/SKILL.md --emit-critique-prompt \`
- Line 25: `skillcheck /tmp/anthropics-skills/skills/mcp-builder/SKILL.md \`
- Line 31: `skillcheck /tmp/uxuiprinciples-skills/ --strict-vscode --format text`

### docs/implementation-status.md

Classification: current repository URLs and migration documentation.

- Line 3: `Operative plan: [original DOCX](SkillCheck_Technical_Debt_and_Product_Plan.docx). Started 2026-09-12 at 'b540a3f047bf411170e8cb95bbe630ca7bf96c14' (1.5.0). The plan was read in full, including both tables and package XML. Instruction discovery covered ancestors and hidden/scoped case variants; '.github/CLAUDE.md' is the only repository instruction file. Contributor guidance, architecture, workflows and historical remediation were read. The supplied untracked DOCX is preserved.`
- Line 11: `- Name inventory captured before edits. Current remote remains 'moonrunnerkc/skillcheck'; no remote changes or publishing authorized.`

### docs/migration.md

Classification: current repository URLs and migration documentation.

- Line 1: `# Migrating SkillCheck to TraceMantle`
- Line 3: `TraceMantle is a different distribution. Installing it is not an automatic upgrade of 'skillcheck'. Before uninstalling anything, verify that the installed 'skillcheck' distribution is this project's old analyzer, not an unrelated package or executable with the same name. Prefer a clean virtual environment.`
- Line 8: `python -m pip show skillcheck`
- Line 10: `python -m pip uninstall skillcheck`
- Line 16: `Do not install the old and new distributions together: compatibility wrappers occupy the old Python package's paths. The new distribution installs only the 'tracemantle' console script. If a private shell workflow needs the old command temporarily, an explicit shell alias 'alias skillcheck=tracemantle' is an opt-in option; do not replace another product's command.`
- Line 18: `The documented top-level imports ('validate', 'Diagnostic', 'Severity', 'ValidationResult', 'ParsedSkill', 'ParseError', '__version__') and the core/agents imports have thin deprecated wrappers. Prefer 'from tracemantle import validate'. Importing 'skillcheck' emits a 'DeprecationWarning'. Old internal configuration mutators were removed; pass immutable 'DocumentSettings' to 'parse()' or 'validate()' instead. There is no legacy console-script installation or 'python -m skillcheck' entry point.`
- Line 20: `Configuration is merged per field at the selected project root: defaults, 'skillcheck.toml' and '[tool.skillcheck]', '[tool.tracemantle]', 'tracemantle.toml', then explicit CLI arguments. Canonical standalone fields take precedence over canonical pyproject fields. Legacy use warns on stderr, preserving machine stdout. Rename the standalone file or move its fields under '[tool.tracemantle]'; frontmatter options go under '[tool.tracemantle.frontmatter]'. An explicit '--config' selects one file for the invocation. Projects with different discovered config roots require separate invocations.`
- Line 24: `Legacy '.skillcheck-history.json' files remain readable. To migrate, select a destination outside the evaluated skill directory:`
- Line 27: `tracemantle migrate-history skill/.skillcheck-history.json --store evidence/legacy --format json`
- Line 32: `New '--history' writes one immutable record per run outside the bundle. Its name is a full SHA-256 of its record, including a unique run ID, so concurrent writers cannot replace each other's records. History record schema 2 retains the v1 entry field 'skillcheck_version'; it is a historical data identifier, not the current distribution name. Legacy 'save_ledger'/'append_run' imports remain compatibility APIs for explicitly requested v1 writes, with their historical single-writer limitation; the CLI uses the immutable store.`

### docs/rename-checklist.md

Classification: current repository URLs and migration documentation.

- Line 3: `Implementation uses TraceMantle internally. The current repository remains 'moonrunnerkc/skillcheck'. Intended owner-managed destination: 'moonrunnerkc/tracemantle'. No remote rename, remote change, publication, release or deployment was performed during implementation.`
- Line 5: `- Confirm ownership/availability of the **tracemantle** PyPI distribution. Provision it as a different project; never publish these artifacts under **skillcheck**.`
- Line 9: `- Update installed skill examples and consumers' 'uses: moonrunnerkc/skillcheck@...' references to 'moonrunnerkc/tracemantle@...' after the new release tag exists. Existing tags can still contain old package/action code; a remote rename does not rewrite them.`
- Line 10: `- Configure the **tracemantle** PyPI trusted publisher with owner **moonrunnerkc**, repository **tracemantle**, workflow **release.yml**, environment **pypi**. If testing publishing before the remote rename, use repository **skillcheck** for this new distribution and update the publisher after renaming.`

### pyproject.toml

Classification: current repository URLs and migration documentation.

- Line 35: `Repository = "https://github.com/moonrunnerkc/skillcheck"`
- Line 36: `Issues = "https://github.com/moonrunnerkc/skillcheck/issues"`
- Line 57: `packages = ["src/tracemantle", "src/skillcheck"]`

### scripts/benchmark.py

Classification: baseline/migration tooling or current repository reference.

- Line 65: `bootstrap = 'import skillcheck.tokenizer as t; t._tiktoken_available=True; t._tiktoken_enc=None; from skillcheck.cli import main; main()'`

### scripts/tracemantle_case_study_report.py

Classification: baseline/migration tooling or current repository reference.

- Line 189: `history_files = sorted(CORPUS.glob("**/.skillcheck-history.json"))`

### scripts/verify_artifacts.py

Classification: baseline/migration tooling or current repository reference.

- Line 44: `run([*pip, 'uninstall', '-y', 'skillcheck'], work)`
- Line 47: `old_executable = environment / ('Scripts/skillcheck.exe' if os.name == 'nt' else 'bin/skillcheck')`
- Line 49: `raise ValueError('Clean install must expose tracemantle and no skillcheck executable.')`
- Line 58: `import skillcheck`
- Line 59: `assert skillcheck.validate is tracemantle.validate`
- Line 61: `from skillcheck.core import extract_graph_heuristic`

### src/skillcheck/__init__.py

Classification: deprecated Python import wrappers and stable schemas.

- Line 14: `warnings.warn("skillcheck imports are deprecated; import tracemantle instead", DeprecationWarning, stacklevel=2)`

### src/skillcheck/schemas/critique-v1.json

Classification: deprecated Python import wrappers and stable schemas.

- Line 3: `"$id": "https://raw.githubusercontent.com/moonrunnerkc/skillcheck/main/src/skillcheck/schemas/critique-v1.json",`

### src/skillcheck/schemas/graph-v1.json

Classification: deprecated Python import wrappers and stable schemas.

- Line 3: `"$id": "https://raw.githubusercontent.com/moonrunnerkc/skillcheck/main/src/skillcheck/schemas/graph-v1.json",`

### src/tracemantle/bundle.py

Classification: legacy schema/history/config/API compatibility.

- Line 18: `EXCLUDED = frozenset({'.git', '__pycache__', '.DS_Store', '.skillcheck-history.json'})`

### src/tracemantle/cli.py

Classification: legacy schema/history/config/API compatibility.

- Line 322: `print("Deprecated SkillCheck configuration: migrate to [tool.tracemantle] or tracemantle.toml.", file=sys.stderr)`

### src/tracemantle/commands.py

Classification: legacy schema/history/config/API compatibility.

- Line 372: `legacy = target_path.parent / '.skillcheck-history.json'`

### src/tracemantle/config_loader.py

Classification: legacy schema/history/config/API compatibility.

- Line 1: `"""Project configuration with a deprecated SkillCheck transition."""`
- Line 88: `"""Find skillcheck.toml from a path or one of its parents.`
- Line 99: `Path to skillcheck.toml, or None when not found.`
- Line 107: `candidate = directory / "skillcheck.toml"`
- Line 167: `legacy = tool.get("skillcheck", {})`
- Line 221: `paths = [directory / name for name in ("skillcheck.toml", "pyproject.toml", "tracemantle.toml")]`
- Line 232: `if not any(name in namespaces for name in ("tracemantle", "skillcheck")):`
- Line 251: `legacy_used |= path.name == "skillcheck.toml"`
- Line 254: `legacy_used |= "skillcheck" in tomllib.loads(raw).get("tool", {})`

### src/tracemantle/core/history.py

Classification: legacy schema/history/config/API compatibility.

- Line 82: `skillcheck_version: str    # From __version__`
- Line 157: `skillcheck_version=version,`
- Line 239: `"skillcheck_version": entry.skillcheck_version,`
- Line 301: `lines.append(f"         version={run.skillcheck_version}  hash={run.skill_content_hash}")`

### src/tracemantle/core/history_io.py

Classification: legacy schema/history/config/API compatibility.

- Line 3: `Serialization, atomic writes, and load/append of ''.skillcheck-history.json''`
- Line 43: `for name in ('timestamp_utc', 'skillcheck_version', 'skill_content_hash'):`
- Line 74: `skillcheck_version=data["skillcheck_version"],`
- Line 105: `path: Path to a ''.skillcheck-history.json'' file.`
- Line 198: `fd, tmp_path = tempfile.mkstemp(dir=path.parent, prefix=".skillcheck-tmp-")`
- Line 236: `path: Path to the ''.skillcheck-history.json'' ledger file.`

### src/tracemantle/io_limits.py

Classification: legacy schema/history/config/API compatibility.

- Line 17: `# skillcheck.toml holds a handful of scalar defaults and an extension-field list.`

### src/tracemantle/schemas/critique-v1.json

Classification: legacy schema/history/config/API compatibility.

- Line 3: `"$id": "https://raw.githubusercontent.com/moonrunnerkc/skillcheck/main/src/skillcheck/schemas/critique-v1.json",`

### src/tracemantle/schemas/graph-v1.json

Classification: legacy schema/history/config/API compatibility.

- Line 3: `"$id": "https://raw.githubusercontent.com/moonrunnerkc/skillcheck/main/src/skillcheck/schemas/graph-v1.json",`

### tests/fixtures/history/ledger_multi_run_passing.json

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 7: `"skillcheck_version": "0.2.0",`
- Line 28: `"skillcheck_version": "0.2.0",`
- Line 49: `"skillcheck_version": "0.2.0",`

### tests/fixtures/history/ledger_one_run.json

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 7: `"skillcheck_version": "0.2.0",`

### tests/fixtures/history/ledger_with_regression.json

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 7: `"skillcheck_version": "0.2.0",`
- Line 28: `"skillcheck_version": "0.2.0",`

### tests/test_build_plan_acceptance.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 91: `config.write_text('[tool.skillcheck]\nmax-lines=20\n[tool.tracemantle]\nmax_lines=30\n')`

### tests/test_build_plan_core.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 110: `(tmp_path / 'skillcheck.toml').write_text('max-lines=20\nmax-tokens=100\n[frontmatter]\nreserved_words=["legacy"]\n')`
- Line 122: `(tmp_path / 'skillcheck.toml').write_text('format="json"\ntarget-agent="vscode"\n')`
- Line 128: `assert 'Deprecated SkillCheck' in proc.stderr`
- Line 132: `(tmp_path / 'skillcheck.toml').write_text('format="text"\nformat="json"\n')`

### tests/test_build_plan_product.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 185: `source = bundle / '.skillcheck-history.json'`

### tests/test_cli_history.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 171: `expected = {"timestamp_utc", "skillcheck_version", "skill_content_hash",`
- Line 282: `"skillcheck_version": "0.2.0",`

### tests/test_config_loader.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 1: `"""Tests for skillcheck.toml discovery and parsing (config_loader)."""`
- Line 25: `cfg = _write(tmp_path / "skillcheck.toml", 'max-lines = "nope"\n')`
- Line 31: `cfg = _write(tmp_path / "skillcheck.toml", 'strict-vscode = "yes"\n')`
- Line 37: `cfg = _write(tmp_path / "skillcheck.toml", "format = 5\n")`
- Line 55: `_write(tmp_path / "skillcheck.toml", "max-lines = 10\n")`
- Line 67: `cfg = _write(repo / "skillcheck.toml", "max-lines = 10\n")`
- Line 81: `cfg = _write(skill_dir / "skillcheck.toml", "max-lines = 900\n")`

### tests/test_config_validation.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 1: `"""Every rejection path in skillcheck.toml loading produces a usable message.`
- Line 23: `path = tmp_path / "skillcheck.toml"`
- Line 174: `config = tmp_path / "skillcheck.toml"`
- Line 184: `(tmp_path / "skillcheck.toml").write_text("max_lines = 100\n", encoding="utf-8")`
- Line 187: `nearest = inner / "skillcheck.toml"`

### tests/test_fail_on_regression.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 41: `skillcheck_version="1.2.0",`
- Line 53: `skillcheck_version="1.2.0",`
- Line 83: `skillcheck_version="1.2.0",`
- Line 92: `skillcheck_version="1.3.0",`
- Line 109: `skillcheck_version="1.2.0",`
- Line 118: `skillcheck_version="1.3.0",`

### tests/test_frontmatter.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 132: `"""[frontmatter] reserved_words in skillcheck.toml replaces the default`
- Line 138: `toml = tmp_path / "skillcheck.toml"`

### tests/test_history_io.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 68: `lp = tmp_path / ".skillcheck-history.json"`
- Line 73: `lp = tmp_path / ".skillcheck-history.json"`
- Line 80: `lp = tmp_path / ".skillcheck-history.json"`
- Line 87: `lp = tmp_path / ".skillcheck-history.json"`
- Line 149: `lp = tmp_path / ".skillcheck-history.json"`
- Line 160: `lp = tmp_path / ".skillcheck-history.json"`
- Line 162: `orphan = tmp_path / ".skillcheck-tmp-orphan"`
- Line 177: `lp = tmp_path / ".skillcheck-history.json"`
- Line 203: `lp = tmp_path / ".skillcheck-history.json"`
- Line 212: `expected = ["timestamp_utc", "skillcheck_version", "skill_content_hash",`
- Line 227: `lp = tmp_path / ".skillcheck-history.json"`
- Line 239: `lp = tmp_path / ".skillcheck-history.json"`
- Line 263: `lp = tmp_path / ".skillcheck-history.json"`

### tests/test_history_model.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 147: `assert entry.skillcheck_version == "0.2.0"`
- Line 225: `lp = tmp_path / ".skillcheck-history.json"`
- Line 236: `lp = tmp_path / ".skillcheck-history.json"`
- Line 254: `lp = tmp_path / ".skillcheck-history.json"`

### tests/test_history_regression.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 61: `skillcheck_version=entry.skillcheck_version,`

### tests/test_io_limits.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 5: `skillcheck.toml were read with a bare 'path.read_text()'. In the case that`
- Line 105: `ledger = _write_bytes(tmp_path / ".skillcheck-history.json", MAX_LEDGER_BYTES + 1)`
- Line 113: `config = _write_bytes(tmp_path / "skillcheck.toml", MAX_CONFIG_BYTES + 1)`
- Line 121: `config = tmp_path / "skillcheck.toml"`
- Line 128: `assert load_ledger(tmp_path / ".skillcheck-history.json") is None`

### tests/test_untrusted_input.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 4: `skillcheck.toml containing non-UTF-8 bytes raised UnicodeDecodeError straight`
- Line 6: `the worse of the two: 'find_config' discovers skillcheck.toml by walking up from`
- Line 194: `ledger = tmp_path / ".skillcheck-history.json"`
- Line 202: `ledger = tmp_path / ".skillcheck-history.json"`
- Line 210: `ledger = tmp_path / ".skillcheck-history.json"`
- Line 217: `config = tmp_path / "skillcheck.toml"`
- Line 225: `config = tmp_path / "skillcheck.toml"`
- Line 233: `config = tmp_path / "skillcheck.toml"`
- Line 246: `config = tmp_path / "skillcheck.toml"`
- Line 267: `of a tree containing a binary skillcheck.toml.`
- Line 270: `(tmp_path / "proj" / "skillcheck.toml").write_bytes(b"\xff\xfe")`
- Line 279: `(tmp_path / "proj" / ".skillcheck-history.json").write_bytes(b"\xff\xfe")`

### tests/test_v1_2_false_positive_fixes.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 72: `loaded_config = load_config(fixture_dir / "skillcheck.toml")`

### tests/test_v1_completion.py

Classification: legacy compatibility and migration tests / historical fixtures.

- Line 69: `(tmp_path / "skillcheck.toml").write_text(`
