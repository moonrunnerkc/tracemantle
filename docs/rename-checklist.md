# Owner checklist for the repository rename

The owner renamed the repository to [moonrunnerkc/tracemantle](https://github.com/moonrunnerkc/tracemantle). Post-rename inspection on 2026-09-12 confirmed the destination and reviewed the settings below. The rename work is complete. The separately authorized 1.6.0 publication is complete and verified below.

- [x] Verify the new repository URL (HTTP 200) and the old `moonrunnerkc/skillcheck` URL (HTTP 301 to the new repository).
- [x] Update `origin` to `https://github.com/moonrunnerkc/tracemantle.git`, preserving its existing HTTPS transport. Fetch and remote-main inspection succeeded.
- [x] Update package Repository/Issues metadata, README clone/Action examples, migration-guide pre-commit examples, CI/license/PyPI badges and migration instructions. The README uses the requested Best-README-Template structure with brief user-facing setup and usage guidance. Installed skill path: `skills/tracemantle/SKILL.md`.
- [x] Preserve historical changelog/remediation/case-study records, deprecated imports, legacy config/history identifiers and stable v1 schema IDs. Keep `SkillCheck_Technical_Debt_and_Product_Plan.docx` unchanged. The [pre-rename name audit](verification/name-audit.md) is a historical snapshot, not a current link inventory.
- [x] Review GitHub access and protections: authenticated account `moonrunnerkc` has admin access; default branch `main` has no branch protection and the repository has no rulesets. No PR is required. Actions are enabled, all actions are allowed, the default workflow token is read-only and workflow PR approvals are disabled. No protection was bypassed.
- [x] Review the `pypi` environment: version tags matching `v*.*.*` are now the only allowed deployment refs. Existing settings remain: no required reviewers or wait timer, and administrator bypass is allowed.
- [x] Keep publishing disabled: set repository variable `TRACEMANTLE_PUBLISH_ENABLED=false` and disable the Release workflow (`disabled_manually`). Both safeguards were enabled only for the separately authorized 1.6.0 release, then disabled again. Ordinary CI remains available.
- [x] Review release automation: only immutable version-tag pushes trigger Release; the publish job depends on the full quality workflow and the enable variable. Moving `v1` depends on successful publishing. Release-note promotion runs only after a public release and opens a changelog PR. GitHub Latest is now `v1.6.0`; `v1` points to its successfully published source. Older version tags/releases remain historical.
- [x] Update the attestation verification example to require `--repo moonrunnerkc/tracemantle` and `--source-digest FULL_RELEASE_COMMIT_SHA`. Both 1.6.0 artifacts passed actual attestation verification against the release source SHA; ordinary CI does not attest artifacts.
- [x] Run local acceptance: CPython 3.10 full release gate, CPython 3.12 optional tokenizer, calibration/conformance, generated docs, local links and unchanged plan bytes passed.
- [x] Commit and push the intended implementation to `main` with a short commit message.
- [x] Run full remote CI and fix its failures. The initial run exposed a missing `tomli` development dependency; packaging correctly skipped. After the fix, [CI run 34726120556](https://github.com/moonrunnerkc/tracemantle/actions/runs/34726120556) passed all 15 jobs on `fe7faa37f446dc817b292e3a2e103775a62e6a0f`, including the full matrix and clean wheel/source installs. [Implementation status](implementation-status.md) records the evidence. The final documentation commit also triggers full CI and must pass before handoff; its exact SHA and run are included in that report.
- [x] Update GitHub About to describe skill validation, bundle changes and trusted release evidence; refresh topics for the current project. Create the authorized 1.6.0 public release, mark it Latest and attach the verified PyPI artifacts and checksums.
- [x] Apply the owner's requested author name `moonrunnerkc` to local Git identity and active project attribution; rewrite only the three task commits with the requested author/committer name using an explicit lease, preserving their content, dates and messages. Earlier history, tags and other contributors remain unchanged.
- [x] Verify the **tracemantle** PyPI trusted publisher through the successful OIDC upload and both public PyPI provenance records: owner **moonrunnerkc**, repository **tracemantle**, workflow **release.yml**, environment **pypi**. The configured identity already matched; no account setting change was needed. Direct account UI access remains unavailable, but publication is not blocked.

## Publisher status

TraceMantle 1.6.0 is available on [PyPI](https://pypi.org/project/tracemantle/1.6.0/). The [release verification record](verification/release-1.6.0.json) links each distribution's provenance and records its SHA-256. CI, PyPI and GitHub release artifacts match exactly. A fresh public-index installation passed `pip check`, CLI/module version and fixture validation checks.

Keep both publishing safeguards disabled outside an explicitly authorized release. Future publisher changes belong in [project publishing settings](https://pypi.org/manage/project/tracemantle/settings/publishing/). No further owner action is needed for 1.6.0. Updating consumers in other repositories remains their maintainers' responsibility; no downstream changes are claimed here.

Intentional old-name inventory: historical changelog and remediation records; labeled historical case studies; original plan filename; v1 history keys and filenames; accepted legacy config names; deprecated import wrappers; stable v1 schema IDs/compatibility copies; recorded baseline outputs and pre-rename audit snapshots. Review every newly introduced old-name occurrence against this list.

## Authorized 1.6.0 release

The owner explicitly authorized publication on 2026-09-13 UTC. Immutable tag `v1.6.0` identifies `3f0363cbc348be7f2d77bc3c0a5e2da19d9e7844`. Its [pre-tag CI](https://github.com/moonrunnerkc/tracemantle/actions/runs/34727283535) passed all 15 jobs. The [Release workflow](https://github.com/moonrunnerkc/tracemantle/actions/runs/34727482766) passed all 17 jobs, including the repeated full quality gate, trusted publishing, attestations and the dependent `v1` update.

The [GitHub release](https://github.com/moonrunnerkc/tracemantle/releases/tag/v1.6.0) is public and marked Latest. [Release-note automation](https://github.com/moonrunnerkc/tracemantle/actions/runs/34727751906) passed; the changelog already contained the released entries. README installation and Action examples, migration/pre-commit guidance and PyPI badges now target the released distribution. The original plan bytes and compatibility contracts remain intact. The post-release documentation commit receives its own full remote CI; its exact SHA and completed run are reported at handoff without moving the release tag.
